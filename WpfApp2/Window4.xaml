<Window x:Class="WpfApp2.Window4"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="Window4" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!-- прогресс -->
            <RowDefinition Height="*"/>
            <!-- вопросы -->
            <RowDefinition Height="Auto"/>
            <!-- кнопки -->
        </Grid.RowDefinitions>

        <!-- Заголовок теста -->
        <TextBlock Grid.Row="0"
                   HorizontalAlignment="Left" 
                   Margin="30,10,0,10" 
                   TextWrapping="Wrap" 
                   VerticalAlignment="Center" 
                   Width="258" 
                   FontSize="19">
            <Run Text="Тест"/>
            <Run Text=":"/>
            <Run Text=")"/>
            <LineBreak/>
            <Run Text="выбери правильный ответ"/>
            <Run Language="ru-ru" Text=".."/>
        </TextBlock>

        <!-- Кнопка "Начать тест" -->
        <Button Grid.Row="0" 
                Content="Начать тест" 
                HorizontalAlignment="Right" 
                Margin="0,10,30,10" 
                VerticalAlignment="Center" 
                Height="38" 
                Width="154" 
                FontSize="20"
                x:Name="StartButton"
                Click="StartButton_Click"/>

        <!-- Панель теста (скрыта изначально) -->
        <Grid Name="TestPanel" 
              Grid.Row="2" 
              Visibility="Collapsed"
              Margin="5">
            <!-- Внутренняя структура TestPanel -->
        </Grid>

        <!-- Прогресс -->
        <ProgressBar Grid.Row="1" 
                     Name="ProgressBar" 
                     Margin="30,10,30,10" 
                     Height="10" 
                     Background="#ecf0f1" 
                     Foreground="#3498db" 
                     ValueChanged="ProgressBar_ValueChanged"/>

        <!-- Контейнер для вопросов -->
        <Border Grid.Row="2" 
                Background="White" 
                Margin="30,10,30,10" 
                CornerRadius="5" 
                BorderThickness="1" 
                BorderBrush="#bdc3c7">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                <StackPanel Name="QuestionPanel">
                    <!-- Текст вопроса будет добавляться динамически -->
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Кнопки навигации -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,10,0,10">
            <Button Content="следующий вопрос" 
                    Height="30" 
                    Width="150" 
                    Click="Button_Click"
                    x:Name="NextButton"/>
            <Button Content="завершить тест" 
                    Height="30" 
                    Width="150" 
                    Margin="10,0,0,0"
                    x:Name="SubmitButton"
                    Click="SubmitButton_Click"/>
        </StackPanel>
        <Button Content="назад в Main" HorizontalAlignment="Left" Margin="400,0,0,0" VerticalAlignment="Center" Height="39" Width="117" Click="Button_Click_1"/>
    </Grid>
</Window>